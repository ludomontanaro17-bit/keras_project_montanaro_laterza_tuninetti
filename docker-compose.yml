# docker-compose.yml
version: '3.8'

services:
  wine-api:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./model:/app/model  # ← monta la cartella model dalla root